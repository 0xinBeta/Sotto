{
  "name": "@sotto/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "eval": "tsc -p tsconfig.json && node dist/run.js",
    "start": "node dist/run.js"
  },
  "dependencies": {
    "@sotto/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "7.0.2"
  }
}
